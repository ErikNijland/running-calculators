import {PaceFormValue} from './pace-form-value';
import {DistanceFormValue} from './distance-form-value';

export interface TimeCalculatorValue {
  pace: PaceFormValue;
  distance: DistanceFormValue;
}
